<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".FinanceApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Recurring income reminder service (Hilt-injected) -->
        <service
            android:name=".util.RecurringIncomeReminderService"
            android:exported="false" />

        <!-- Smart financial alerts service (Hilt-injected; started by ViewModels + daily alarm) -->
        <service
            android:name=".util.FinancialAlertsService"
            android:exported="false" />

        <!-- BroadcastReceiver for AlarmManager alarms and BOOT_COMPLETED -->
        <receiver
            android:name=".util.RecurringIncomeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.financeapp.ACTION_RECURRING_INCOME_DUE" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver for the daily 8 PM digest alarm + BOOT_COMPLETED reschedule -->
        <receiver
            android:name=".util.FinancialAlertsReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.financeapp.ACTION_DAILY_DIGEST_ALARM" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
